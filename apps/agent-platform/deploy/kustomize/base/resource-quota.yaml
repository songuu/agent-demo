apiVersion: v1
kind: ResourceQuota
metadata:
  name: agent-platform
  namespace: agent-platform
spec:
  hard:
    requests.cpu: "20"
    requests.memory: 40Gi
    limits.cpu: "50"
    limits.memory: 100Gi
    pods: "100"
    persistentvolumeclaims: "0"
