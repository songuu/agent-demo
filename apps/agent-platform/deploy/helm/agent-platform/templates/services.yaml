apiVersion: v1
kind: Service
metadata:
  name: agent-api
spec:
  type: ClusterIP
  selector: {app: agent-api}
  ports:
    - {name: http, port: 8080, targetPort: http}
    - {name: metrics, port: 9464, targetPort: http}
